#include <iostream>
#include "../Project1/Zergling.h"
#include "../Project1/Hydralisk.h"
#include "../Project1/Vulture.h"
#include "../Project1/Beacon.h"

using namespace std;

int main()
{

#pragma region 업 캐스팅

	//Zerg* juggling = new Zergling();
	//Zerg* hydra = new Hydralisk();
	//Zerg* lurker = new Lurker();

	//juggling->SetHP(10);
	//hydra->SetHP(15);
	//lurker->SetHP(20);

	//cout << "juggling의 현재 체력 " << juggling->GetHP() << endl;
	//cout << "hydra의 현재 체력 " << hydra->GetHP() << endl;
	//cout << "lurker의 현재 체력 " << lurker->GetHP() << endl;

	//Beacon beacon;

	//beacon.Trigger(juggling);
	//beacon.Trigger(hydra);
	//beacon.Trigger(lurker);

	//cout << "juggling의 현재 체력 " << juggling->GetHP() << endl;
	//cout << "hydra의 현재 체력 " << hydra->GetHP() << endl;
	//cout << "lurker의 현재 체력 " << lurker->GetHP() << endl;


#pragma endregion


	return 0;
}

